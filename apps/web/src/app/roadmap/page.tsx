/**
 * Roadmap. SCAFFOLD: features grouped by `roadmapQuarter`. Wire to `features`
 * grouped by quarter with assignee/status chips.
 */
export default function RoadmapPage() {
  return (
    <section>
      <h1>Roadmap</h1>
      <p style={{ color: "#9ca3af" }}>No roadmap items yet (scaffold).</p>
    </section>
  );
}
